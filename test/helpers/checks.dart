/// Re-export `package:checks` so tests can import it from test_imports.dart.
///
/// Prefer `checks` for expressive assertions when it improves readability.
library;

export 'package:checks/checks.dart';
