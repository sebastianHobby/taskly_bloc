# Copy to .env and adjust if needed.
#
# The default values assume:
# - Supabase CLI project_id = taskly_bloc (see supabase/config.toml)
# - Supabase local stack is running (`supabase start`)

# PowerSync API port exposed to host
PS_PORT=8080

# Supabase JWKS endpoint via Kong container (reachable only on Supabase docker network)
PS_JWKS_URL=http://kong:8000/auth/v1/.well-known/jwks.json

# Postgres inside the Supabase docker network
# Container name format: supabase_db_<project_id>
PS_DATA_SOURCE_URI=postgres://postgres:postgres@supabase_db_taskly_bloc:5432/postgres

# Mongo for PowerSync bucket storage
PS_MONGO_URI=mongodb://mongo:27017/powersync

# Supabase docker network name (derived from supabase_network_<project_id>)
SUPABASE_DOCKER_NETWORK=supabase_network_taskly_bloc
