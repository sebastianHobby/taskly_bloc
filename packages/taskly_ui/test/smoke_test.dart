@Tags(['unit'])
library;

import 'package:flutter_test/flutter_test.dart';

import 'helpers/test_helpers.dart';

void main() {
  testSafe('taskly_ui test harness boots', () async {
    expect(1, 1);
  });
}
